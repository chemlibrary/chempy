from setuptools import setup, find_packages

setup(
    name="chempy",
    version="2.1.2",
    packages=find_packages(),
    install_requires=[],
    author="Chem",
    description="Chems Python library.",
    python_requires=">=3.8",
)
